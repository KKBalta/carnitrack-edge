# ═══════════════════════════════════════════════════════════════════════════════
# CarniTrack Edge - Docker Environment Configuration
# ═══════════════════════════════════════════════════════════════════════════════
# 
# Usage: 
#   docker compose --env-file .env.docker up -d
#
# ═══════════════════════════════════════════════════════════════════════════════

# Edge Identity
EDGE_NAME=CarniTrack-Edge-Docker
SITE_ID=SITE-DOCKER-01
REGISTRATION_TOKEN=test-token-docker

# Cloud Connection - Mock server on host machine
CLOUD_API_URL=http://host.docker.internal:3001/api/v1/edge

# Servers
TCP_PORT=8899
TCP_HOST=0.0.0.0
HTTP_PORT=3000
HTTP_HOST=0.0.0.0

# Database (persisted via Docker volume)
DB_PATH=/app/data/carnitrack.db

# Logging
LOG_LEVEL=debug
NODE_ENV=development
